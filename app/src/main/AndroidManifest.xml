<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!--
        v3.4 foreground service + Quick Settings tile are temporarily
        unbound from the manifest while investigating a fresh-install
        startup crash. The Kotlin classes (FlashForegroundService,
        FlashTileService, AndroidFocusServiceController) remain in the
        source tree; only their system entry points are disabled so the
        process can come up clean and we can re-enable them one at a
        time once the trigger is identified.
    -->
    <!-- <uses-permission android:name="android.permission.FOREGROUND_SERVICE" /> -->
    <!-- <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" /> -->

    <application
        android:name=".AvangardApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/Theme.Avangard">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Avangard">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".sync.scheduler.EveningCloseReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.avangard.app.alarm.EVENING_CLOSE" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".sync.scheduler.BootCompletedReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!--
            v3.4 ongoing-notification service. Disabled in manifest while
            diagnosing fresh-install startup crash; re-enable once
            isolated.
        -->
        <!--
        <service
            android:name=".sync.service.FlashForegroundService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="operator-initiated cognitive focus session timer" />
        </service>

        <service
            android:name=".sync.tile.FlashTileService"
            android:exported="true"
            android:icon="@drawable/ic_flash_tile"
            android:label="@string/tile_flash_label"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>
        -->
    </application>

</manifest>
